#include "main.h"

/**
 * _abs -The absolut value of an interger.
 * @c: The number to be computed
 *
 * Retrun: Absolut value of number or 0
 */
int _abs(int c)
{
	if (c < 0)
	{
		int ans_val;

		abs_val = c * -1;

		return (abs_val);
	}

	return (0);
}


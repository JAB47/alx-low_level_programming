#include <stdio.h>

/**
 * main - infinite loop
 *
 * Return: always 0 on success
 */

int main(void)
{
	int i;

	printf("infinite loop incoming :(\n");

	i = 0;
/**
 *      while (i < 10)
 *              putchar(i);
 */
	printf("infinite loop avoided! \\o/\n");

	return (0);
}

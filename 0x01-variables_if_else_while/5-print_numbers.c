#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *main-beginning
 *Description-Prints all decimal numbers
 *Retrun: always 0 is sucess
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
